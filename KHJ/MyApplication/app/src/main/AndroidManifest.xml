<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.ac.sungkyul.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".navigation.CustomNavigationActivity" />
        <activity android:name=".calculator.CalculatorActivity" />
        <activity android:name=".star.MakeStarActivity" />
        <activity android:name=".circularimage.LoginActivity" />
        <activity android:name=".calculator.abcdadfsa"></activity>
    </application>

</manifest>
